import { createSlice } from "@reduxjs/toolkit";

let itemView = createSlice({
  name : 'itemView',
  initialState : [],
  reducers :{
    
  }
});
export let {} = itemView.actions;
export default itemView;